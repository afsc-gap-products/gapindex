# Helper function to format sql queries

Helper function to format sql queries

## Usage

``` r
stitch_entries(stitch_what = "")
```

## Arguments

- stitch_what:

  string of values to stitch

## Value

a single string that encapsulates the values of `stitch_what` in a
parenthesis.
